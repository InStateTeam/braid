{
  "name": "hermes-server",
  "private": true,
  "version": "1.0.0",
  "description": "npm wrapper for running this server within docker",
  "scripts": {
    "install": "mkdir -p ~/.m2 && docker run --rm -v $HOME/.m2:/home/user/.m2 -v $PWD:/build -w /build mfriedenhagen/docker-maven mvn clean install",
    "start": "docker run --rm -p 127.0.0.1:8080:8080 -v $HOME/.m2:/home/user/.m2 -v $PWD:/build -w /build mfriedenhagen/docker-maven mvn exec:exec"
  },
  "author": "",
  "license": "ISC"
}
